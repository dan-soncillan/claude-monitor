{
  "name": "@claude-monitor/slack",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun --env-file=../../.env --watch src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "start": "bun src/index.ts",
    "lint": "tsc --noEmit"
  },
  "dependencies": {
    "@slack/bolt": "^4.2.0",
    "@claude-monitor/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "bun-types": "^1.2.0"
  }
}
